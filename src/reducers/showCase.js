const showCaseItemsReducerDefaultState = ['VanillaJS','React','Redux', 'PWA'];
export default (state = showCaseItemsReducerDefaultState, action) => {
    switch (action.type) {
        default: 
            return state;
    }
};